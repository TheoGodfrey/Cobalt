Safety Monitor Configuration (Component 12) Loaded by drone/main.py and passed to the SafetyMonitorList of safety rules to enforce.The SafetyMonitor will check these on every update.rules:rule: "check_min_battery"enabled: truepriority: 1 # 1 = critical, 10 = lowTrigger emergency RTH if battery drops below 15%parameters:min_battery_percent: 15Action to take if rule is violatedaction: "EMERGENCY_RTH"rule: "check_geofence"enabled: truepriority: 1parameters:Simple box geofence [min_lat, min_lon, max_lat, max_lon]fence: [44.5600, -123.2900, 44.5700, -123.2800]Action to take if drone leaves the fenceaction: "EMERGENCY_RTH"rule: "check_max_altitude"enabled: truepriority: 2parameters:Max altitude in meters AGLmax_altitude_agl: 120action: "HOLD_POSITION" # Stop ascendingrule: "check_comms_loss"enabled: truepriority: 3parameters:Time in seconds before declaring hub comms lostcomms_timeout_sec: 30.0This action is handled by the MissionController's failsafe logic,but the SafetyMonitor logs it.action: "LOG_WARNING"